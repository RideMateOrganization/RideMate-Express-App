const RideVisibility = {
  PUBLIC: 'public',
  PRIVATE: 'private',
};

const RideStatus = {
  PLANNED: 'planned',
  ACTIVE: 'active',
  COMPLETED: 'completed',
  CANCELLED: 'cancelled',
};

export { RideVisibility, RideStatus };
